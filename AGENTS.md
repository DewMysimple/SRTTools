# SRTTools 工程约定

## 开始工作

先执行 `git status -sb`，保留已有用户修改。随后阅读：

1. [工程记忆维护协议](wiki_memory/AGENTS.md)。
2. 当前状态的 [项目概览](wiki_memory/当前状态/项目概览.md)、[系统架构](wiki_memory/当前状态/系统架构.md)、[当前约束](wiki_memory/当前状态/当前约束.md)、[当前待办](wiki_memory/当前状态/当前待办.md)。
3. 与任务相关的 active 决策、知识和已知问题；仅需要追溯时查最近 1–3 篇日志。

源码、测试、真实命令结果和用户明确要求是事实依据。记忆用于导航，不代替核验。不默认读取完整历史。

## 产品边界

- Windows 中文桌面应用，静态两个页面：字幕整理、时间范围导出。用户明确否定多选任务组合并要求重新设计；字幕整理以文件列表与导出预览为核心，不恢复任务卡片或重复的输出计划表。
- 添加文件与修改设置后自动预览。显式选择单一导出格式：纯文本 TXT、原字幕副本 SRT、带时间轴 TXT；格式、编码、文件名与内容可见，只有一个导出入口。纯文本自动处理 SRT/TXT；原样两格式保留字节，不应用正文排版或保存编码。界面无法解码时禁止该项导出，先修正读取编码，不允许绕过预览保存另一种内容。
- 文件夹扫描支持当前层/递归，保留相对层级；跳过已选输出子目录、链接与目录联接，不再按英文分类名称排除目录。读取失败和取消必须可见，取消后不自动续跑预览。保存直接进入用户选定目录，不创建 Text/Original/Clean/SRT 等分类目录。副本保留原字节，永不移动来源；预览零写，执行复核来源与输出路径边界，同名自动编号。
- 原样转换必须保持输入字节不变，包括 BOM、编码和换行；CLI 的原样转换保持旧行为，解码失败只限制预览。
- 结构解析不能静默丢字幕；清理 TXT 不得误删正文数字和普通段落。时间用整数毫秒，范围为左闭右开，负时间/空范围结果明确报错。
- 所有输入只读，导出始终新建文件；同名自动编号。先同目录暂存再原子发布，绝不覆盖输入或既有输出。预览后复核来源摘要。
- 批次结果逐文件可见；失败和取消不撤销已完成文件，不把部分成功描述为全部成功。
- 不添加字幕上传、遥测、语义改写、翻译、视频识别或网络服务，除非用户明确要求。
- 测试仅用临时文件与公开演示字幕。不提交用户字幕、私人路径、凭据、运行日志、缓存或本机配置。

## 架构与界面

- Python 3.14、PySide6；版本以 requirements 为准。`subtitles.py` 与 `service.py` 禁止依赖 Qt；GUI 和 CLI 共用业务内核。
- 静态导入保证冻结打包可发现；全局单任务，通过一个受控 QThread 运行。不得在工作线程直接操作控件，退出时必须等任务结束。
- 修改输入/参数使预览失效。长路径和错误完整悬停；表格按窗口宽度分配，普通、小窗口和最大化均可访问操作按钮。日志最新在上。
- 来源或参数变化立即撤销旧预览和导出资格，180 ms 防抖后自动读取；旧计算结果按修订号丢弃，只有最新预览可导出。预览期间可继续改设置，扫描/导出期间锁定写入相关操作。回调由 GUI 主窗口的显式 queued Slot 分派，线程 finished 后 wait 并释放回调，保持全局单线程。取消不自动重启，关闭停止待触发计时器。
- 导出复用不可变 Prepared.data，不重新计算内容。长文每 50,000 字符分页可查看全文，分页说明不进入内容；预览不软换行，原样 CRLF/CR 在界面显示为换行但输出字节不变。保存时选择目录，取消零写；同名不覆盖。重置只恢复文字参数，保留格式、来源、扫描记录和保存目录。日志默认收起，最新在上；进度只在忙碌时显示。CLI 单任务及输出后缀保持兼容。
- 只采用任务所需复杂度，不照搬 ObManage 的 SQLite、仓库发现或事务恢复等无关功能。

## 验证与交付

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe wiki_memory\工具\memory_lint.py index
.\.venv\Scripts\python.exe wiki_memory\工具\memory_lint.py check
.\.venv\Scripts\python.exe tools\build.py
.\dist\SRTTools\SRTTools.exe --smoke-test .\artifacts\frozen\workspace.png
```

按改动检查实时预览、防抖与过期结果、取消/关闭、GUI 回调线程与 GC 压力、三种导出格式、三种正文排版与编码、长文分页全文一致、SRT/TXT 混合输入、递归扫描、批次失败、时间边界、同名保护和陈旧来源。UI 检查 980×620、1180×900、大窗口和最大化，含设置/日志展开和长文件名，确认表格不横向溢出且底部操作可滚动到达。`--smoke-test` 默认离屏，不在用户桌面弹窗或反复调整可见窗口；需要原生桌面验收时在专用测试主机显式设 QT_QPA_PLATFORM=windows。纯文档改动检查链接和记忆格式。

每轮实质修改结束前更新工程记忆与日志，运行必要验证，按最终源码生成 `dist/SRTTools.zip`，显式暂存本轮文件，提交并推送 `origin` 当前分支。用户已授权远程仓库管理，无须反复确认。用本地 HEAD、远程分支哈希和 `git status -sb` 核对交付；不得预写“推送成功”。禁止强推或擅自重写公共历史。新分支使用 `codex/` 前缀。

构建前确认目标为本项目 `dist/SRTTools` 且旧 EXE 已退出，不强制终止用户程序。保留构建脚本的干净子进程 PATH。ZIP 根层直接包含 `SRTTools.exe`、`_internal`、使用说明和许可证，不能多包一层 SRTTools 文件夹。二进制保留在被忽略的 `dist/`，不进入 Git。

未经用户明确要求不启动子代理。`AGENT.md` 只引用本文件，避免指令分叉。
