---
type: log
status: archived
kind: ui
importance: high
updated: 2026-09-20
topic: live-preview-export-parity
source_logs: []
supersedes: null
---

# 2026-09-20｜实时预览与导出一致性

- 时间：2026-09-20（北京时间）
- 类型：ui
- 状态：完成
- 目标：减少拥挤，明确文件形式与类型，设置实时预览且导出与预览一致。
- 日志索引：[[日志/MOC_工作日志|工作日志 MOC]]

## 已确认的决策

- 用户截图与目标针对 SRTTools；修改该工程，未改 ObManage。
- 单一格式选择取代隐藏的其他保存菜单：纯文本 TXT、原字幕 SRT、带时间轴 TXT；展示文件名、编码、类型和实际内容，不恢复多选组合或英文分类目录。
- 两页设置变化防抖自动预览，只应用最新修订；导出直接使用 Prepared.data。原样格式保留字节，不能解码时先修正读取编码，不绕过预览。
- 长文可分页查看全文，不截断，不把分页说明写入文件。日志默认收起、进度忙碌时显示，设置位于预览面板内而非全页大表单。

## 检查与操作

- 初始 main 工作区干净，基线为上一轮 1.3.0。
- 保留只读来源、零写预览、摘要复核、路径边界、同名编号、相对层级、原子发布与单 Worker。
- 回归测试曾两次在 GC 期间进程中止；改用 GUI 主窗口显式 queued Slot 分派，回调强引用直到 finished + wait 后释放。加入 GUI 线程归属、运行中 GC 与连续切换压力测试，未隐藏失败或禁用 GC。
- UI 自动验收全部离屏，仅临时公开演示数据，不触碰用户字幕、不弹出测试窗口。

## 文件变更

- [实时预览与全文阅读](../../srttools/preview.py)、[字幕整理](../../srttools/workbench.py)、[主窗口/范围页](../../srttools/ui.py)、[格式处理](../../srttools/workflow.py)。
- [UI 回归](../../tests/test_ui.py)、[源码/冻结验收](../../srttools/smoke.py)、版本 1.4.0、README、指南、演示截图与有效记忆。

## 测试与验证

- 应用回归 95 项通过，并连续三轮复跑通过；覆盖三排版 × 三输出编码、原样两格式 × BOM/CRLF/GB18030、快速连续编辑、过期成功结果丢弃、错误恢复、取消/关闭、线程归属/GC、长文拼接与导出逐字节相等、范围实时更新。
- 源码、dist 冻结 EXE、ZIP 全新解压 EXE 的离屏验收均退出 0；验证真实自动预览与三格式保存、范围两格式、全文分页，以及 980×620、1180×900、1600×1000、最大化、设置/日志展开和长文件名。最终三份演示截图 SHA-256 一致，人工检查中文、文件类型、预览和按钮可达。
- 12 项记忆工具测试与 17 页记忆检查通过；Git diff 空白检查通过。
- 基于最终运行源码和说明书构建 `dist/SRTTools.zip`：49,900,379 字节；SHA-256 `36ca1d002e78c5f4ce8e121a26ee58b077938747ecb48dc9f6c501b1ffd5ea5a`。根层包含 EXE、_internal、使用说明、licenses 与第三方声明，无 SRTTools 外层目录，构建 CRC 验证通过。解压 EXE 与 dist EXE 哈希一致，解压说明书与当前源码文档一致。

## 待确认长期记忆

- 无；本轮行为基于用户明确目标。

## 问题、结果与下一步

- 实现、验证、说明与最新版便携包已完成；提交推送另以本地 HEAD、远端分支引用与干净工作区核验，不预写远端成功。
- 无待确认功能阻塞；构建产物留在忽略目录，未修改或处理用户字幕。
